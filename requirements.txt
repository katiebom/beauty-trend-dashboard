# ─────────────────────────────────────────────────────────────
# Streamlit Cloud / GitHub Actions / 로컬 공통 의존성
# 버전 유연화 — pip이 호환 버전 자동 선택 (빌드 실패 방지)
# ─────────────────────────────────────────────────────────────

# UI
streamlit>=1.35,<1.45
streamlit-authenticator>=0.3.2,<0.4

# 데이터
pandas>=2.0,<2.3
numpy>=1.24,<2.1
plotly>=5.20,<6

# Sheets/Google
gspread>=6.0,<7
google-auth>=2.28
google-api-python-client>=2.120

# AI
google-genai>=1.0.0

# 트렌드/외부 API
pytrends>=4.9
praw>=7.7
requests>=2.31
beautifulsoup4>=4.12
fake-useragent>=1.4

# 유틸
pyyaml>=6.0
python-dotenv>=1.0
python-dateutil>=2.9
openpyxl>=3.1
bcrypt>=4.0

# PDF (선택, CIR parser용)
pdfplumber>=0.11
